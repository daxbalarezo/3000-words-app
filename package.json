{
  "name": "3000-words-app",
  "version": "1.0.0",
  "description": "Aplicación Full Stack para el aprendizaje de 3000 palabras.",
  "main": "server/server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "seed": "node server/seeder.js",
    "build-client": "cd client && npm cache clean --force && npm install && npm run build", 
    "prestart": "npm run build-client", 
    "start": "node server/server.js"
  },
  "keywords": [
    "mern",
    "react",
    "express",
    "mongodb"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@google-cloud/vertexai": "^1.10.0",
    "@google/generative-ai": "^0.24.1",
    "axios": "^1.12.2",
    "cors": "^2.8.5",
    "dotenv": "^17.2.2",
    "express": "^5.1.0",
    "mongoose": "^8.18.2"
  }
}
